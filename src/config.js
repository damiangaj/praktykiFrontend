const mainUrl = 'http://localhost:8081';

const config = {
    CREATE_FORM_URL: `${mainUrl}/api/create-form`,
    EDIT_FORM_URL: `${mainUrl}/api/edit-form`,
    GET_FORM_URL: `${mainUrl}/api/form`,
    DELETE_FORM_URL: `${mainUrl}/api/delete-form`,

};

export default config;